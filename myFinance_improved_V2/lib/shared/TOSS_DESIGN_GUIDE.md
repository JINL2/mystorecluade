# Toss Design System Guide

> MyFinance 앱을 위한 토스 디자인 시스템 가이드라인
>
> Last Updated: 2025-01-04

---

## 1. Design Philosophy (디자인 철학)

### 토스가 추구하는 핵심 가치

토스 디자인 시스템(TDS)은 단순히 UI 컴포넌트 모음이 아닌, **사용자 신뢰를 구축하는 시각 언어**입니다.

```
"금융 서비스는 신뢰가 전부다.
 복잡함을 숨기고, 명확함을 드러내라."
```

### 7가지 핵심 원칙

| 원칙 | 설명 |
|------|------|
| **1. Minimalist & Clean** | 화이트 도미넌트, 여백을 통한 호흡 |
| **2. Trust & Clarity** | 전문적이고 신뢰감 있는 금융 인터페이스 |
| **3. Single Focus** | 화면당 하나의 주요 액션에 집중 |
| **4. Subtle Depth** | 최소한의 그림자, 경계선으로 구분 |
| **5. Smooth Motion** | 200-250ms 애니메이션, 바운스 없음 |
| **6. Consistent Grid** | 엄격한 4px 스페이싱 시스템 |
| **7. Strategic Color** | CTA는 블루, UI는 그레이스케일 |

---

## 2. Typography (타이포그래피)

### 토스 공식 타이포그래피 스케일

토스는 **Toss Product Sans**를 자체 개발하여 사용합니다.
비슷한 오픈소스 대안: **Pretendard**, **Inter**

#### Font Size Scale

```
┌─────────┬──────────┬─────────────┬─────────────────────┐
│ Token   │ Size     │ Line Height │ 용도                │
├─────────┼──────────┼─────────────┼─────────────────────┤
│ F11     │ 11px     │ 16.5px (1.5)│ 최소 텍스트, 캡션   │
│ F14     │ 14px     │ 21px (1.5)  │ 보조 본문, 설명     │
│ F15     │ 15px     │ 22.5px (1.5)│ 부가 정보           │
│ F17     │ 17px     │ 25.5px (1.5)│ 기본 본문 (영문 권장)│
│ F20     │ 20px     │ 29px (~1.45)│ 섹션 제목           │
└─────────┴──────────┴─────────────┴─────────────────────┘
```

#### Semantic Labels (Title/Subtitle)

```
T1 (30px) → T7 까지 Title 계열
ST1 → ST13 (11px) Subtitle 계열
```

### 영어 앱 권장 사항 (iOS/Android)

| 플랫폼 | Body 기본 | Secondary | Caption |
|--------|-----------|-----------|---------|
| **iOS (HIG)** | 17pt | 15pt | 13pt |
| **Android (Material)** | 16sp | 14sp | 12sp |
| **Web** | 16px | 14px | 12px |
| **토스 (한국어)** | 14px | 13px | 12px |

#### 핵심 원칙: Line Height

```dart
// 토스 표준: 1.5배 Line Height
lineHeight = fontSize * 1.5

// 예시
F14: 14px → lineHeight: 21px
F17: 17px → lineHeight: 25.5px
```

### 현재 프로젝트 vs 권장 스케일

```dart
// 현재 (TossTextStyles)
body: 14px, height: 1.429      // 토스 한국어 기준
subtitle: 16px                  // 비표준 (토스에 F16 없음)

// 영어 앱 권장
body: 16px, height: 1.5        // iOS 17pt에 근접
subtitle: 17px                  // 토스 F17 표준
```

### 타이포그래피 핵심 철학

1. **균형 (Balance)**: 숫자, 한글, 영문 각각 사용 시에도 시각적 균형 유지
2. **금융 맥락**: `%`, `+`, `-` 같은 기호는 더 크게, 아이콘처럼 취급
3. **중립성**: 과도한 특징 절제, 콘텐츠가 주인공

---

## 3. Spacing System (스페이싱 시스템)

### 4px Grid System

토스의 모든 스페이싱은 **4px의 배수**입니다.

```
┌────────────┬─────────┬────────────────────────┐
│ Token      │ Value   │ 용도                   │
├────────────┼─────────┼────────────────────────┤
│ space1     │ 4px     │ 최소 간격, 아이콘-텍스트│
│ space2     │ 8px     │ 타이트한 간격          │
│ space3     │ 12px    │ 작은 간격              │
│ space4     │ 16px    │ 기본 간격 ⭐           │
│ space5     │ 20px    │ 중간 간격              │
│ space6     │ 24px    │ 큰 간격 ⭐             │
│ space8     │ 32px    │ 섹션 간격              │
│ space12    │ 48px    │ 컨테이너 간격          │
└────────────┴─────────┴────────────────────────┘
```

### Padding 패턴

```dart
// 컴포넌트 내부 패딩
paddingXS: 8px    // 작은 버튼, 칩
paddingSM: 12px   // 입력 필드
paddingMD: 16px   // 카드, 리스트 아이템 ⭐
paddingLG: 20px   // 섹션
paddingXL: 24px   // 페이지 패딩 ⭐
```

### Margin 패턴

```dart
// 컴포넌트 사이 마진
marginXS: 4px     // 인라인 요소 사이
marginSM: 8px     // 관련 아이템 사이
marginMD: 16px    // 컴포넌트 사이 ⭐
marginLG: 24px    // 섹션 사이
marginXL: 32px    // 주요 섹션 사이
```

### 스페이싱이 만드는 "토스 느낌"

```
┌─────────────────────────────────────────────────────┐
│                     24px (페이지 패딩)              │
│  ┌───────────────────────────────────────────────┐  │
│  │  16px (카드 내부 패딩)                        │  │
│  │                                               │  │
│  │  Title                        8px ↓          │  │
│  │  ─────────────────────────────────           │  │
│  │                               16px ↓          │  │
│  │  Content Row 1                               │  │
│  │                               12px ↓          │  │
│  │  Content Row 2                               │  │
│  │                                               │  │
│  └───────────────────────────────────────────────┘  │
│                     16px ↓ (카드 사이)              │
│  ┌───────────────────────────────────────────────┐  │
│  │  Next Card...                                │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

---

## 4. Color System (컬러 시스템)

### 브랜드 컬러

```dart
primary:  #0064FF  // 토스 블루 - CTA, 강조
success:  #00C896  // 성공, 수익
error:    #FF5847  // 에러, 손실
warning:  #FF9500  // 경고
```

### 그레이스케일

```dart
gray900:  #202632  // 주요 텍스트
gray800:  #2E3440  // 강조 텍스트
gray700:  #505866  // 보조 텍스트
gray600:  #8B95A1  // 비활성 텍스트
gray400:  #B0B8C1  // 플레이스홀더
gray300:  #D1D6DB  // 구분선
gray100:  #F2F4F6  // 배경
```

### 컬러 사용 원칙

| 용도 | 컬러 | 예시 |
|------|------|------|
| 주요 텍스트 | gray900 | 제목, 금액 |
| 보조 텍스트 | gray700 | 라벨, 설명 |
| 비활성 텍스트 | gray600 | 힌트, 날짜 |
| CTA 버튼 | primary | 제출, 확인 |
| 링크/강조 금액 | primary | 잔액, 링크 |
| 에러/손실 | error | 에러 금액, 오류 메시지 |
| 성공/수익 | success | 수익, 완료 상태 |

---

## 5. Component Heights (컴포넌트 높이)

### 버튼 높이

```dart
buttonHeightSM: 32px   // 작은 버튼 (칩, 태그)
buttonHeightMD: 40px   // 중간 버튼
buttonHeightLG: 48px   // 큰 버튼 ⭐ (기본)
buttonHeightXL: 56px   // 매우 큰 버튼 (CTA)
```

### 입력 필드 높이

```dart
inputHeightSM: 36px    // 작은 입력
inputHeightMD: 44px    // 중간 입력
inputHeightLG: 48px    // 큰 입력 ⭐ (기본)
inputHeightXL: 56px    // 매우 큰 입력
```

### 아이콘 크기

```dart
iconXS: 16px    // 작은 아이콘
iconSM: 20px    // 기본 아이콘
iconMD: 24px    // 중간 아이콘 ⭐
iconLG: 32px    // 큰 아이콘
iconXL: 40px    // 매우 큰 아이콘
```

---

## 6. Border Radius (보더 레디어스)

```dart
xs:   4px    // 작은 요소 (태그, 뱃지)
sm:   6px    // 작은 버튼
md:   8px    // 기본 ⭐
lg:   12px   // 카드, 입력 필드
xl:   16px   // 모달, 바텀시트
xxl:  20px   // 큰 컨테이너
card: 16px   // 카드 전용
```

---

## 7. Shadow System (그림자 시스템)

토스는 **최소한의 그림자**를 사용합니다. 깊이감보다는 경계선으로 구분합니다.

```dart
// 카드 그림자 (매우 subtle)
card: BoxShadow(
  color: gray900.withOpacity(0.04),
  blurRadius: 8,
  offset: Offset(0, 2),
)

// 드롭다운 그림자
dropdown: BoxShadow(
  color: gray900.withOpacity(0.06),
  blurRadius: 12,
  offset: Offset(0, 4),
)

// 모달 그림자
modal: BoxShadow(
  color: gray900.withOpacity(0.08),
  blurRadius: 24,
  offset: Offset(0, 8),
)
```

---

## 8. Animation (애니메이션)

### Duration

```dart
instant: 50ms    // 즉각 반응
quick:   100ms   // 빠른 피드백
fast:    150ms   // 빠른 전환
normal:  200ms   // 기본 전환 ⭐
medium:  250ms   // 중간 전환 ⭐
slow:    300ms   // 느린 전환
slower:  400ms   // 페이지 전환
```

### Curve

```dart
standard: easeInOutCubic  // 기본 전환 ⭐
enter:    easeOutCubic    // 요소 진입 ⭐
exit:     easeInCubic     // 요소 퇴장
emphasis: fastOutSlowIn   // 강조 애니메이션
```

### 애니메이션 원칙

- **No Bounce**: 바운스 효과 사용하지 않음
- **Subtle**: 과하지 않은 미세한 움직임
- **Purpose**: 목적 있는 애니메이션만 사용

---

## 9. Best Practices (베스트 프랙티스)

### Do's (권장)

```dart
// ✅ 일관된 스페이싱 사용
padding: EdgeInsets.all(TossSpacing.space4),  // 16px

// ✅ 시멘틱 컬러 사용
color: TossColors.gray900,  // 주요 텍스트

// ✅ Line Height 1.5배 유지
style: TossTextStyles.body.copyWith(
  fontSize: 16,
  height: 1.5,
),

// ✅ 4px 그리드 준수
SizedBox(height: TossSpacing.space3),  // 12px
```

### Don'ts (지양)

```dart
// ❌ 임의의 스페이싱 사용
padding: EdgeInsets.all(15),  // 4의 배수가 아님

// ❌ 하드코딩된 컬러
color: Color(0xFF333333),  // TossColors 사용

// ❌ 불필요한 그림자
boxShadow: [
  BoxShadow(blurRadius: 20, ...),  // 너무 강함
],

// ❌ 바운스 애니메이션
curve: Curves.bounceOut,  // 토스 스타일 아님
```

---

## 10. 토스 느낌을 만드는 핵심 요소

### 1. 여백의 미학

```
"빈 공간은 낭비가 아니라, 콘텐츠를 돋보이게 하는 무대다."
```

- 넉넉한 페이지 패딩 (24px)
- 카드 내부 충분한 여유 (16px)
- 요소 간 숨 쉴 공간

### 2. 명확한 위계

```
Primary Text:   gray900, 16px, w600 (강조)
Secondary Text: gray700, 16px, w400 (일반)
Tertiary Text:  gray600, 14px, w400 (보조)
```

### 3. 전략적 컬러

```
- 90% 그레이스케일
- 10% 액센트 컬러 (primary, error, success)
- CTA만 컬러풀하게
```

### 4. 일관된 리듬

```
- 모든 것이 4px 그리드
- 예측 가능한 패턴
- 시각적 안정감
```

---

## 11. 참고 자료

### 공식 자료
- [토스 프로덕트 산스 제작기](https://toss.im/tossfeed/article/beginning-of-tps)
- [TDS Mobile 문서](https://tossmini-docs.toss.im/tds-mobile/)
- [토스 디자이너가 제품에만 집중할 수 있는 방법](https://toss.tech/article/toss-design-system)

### 플랫폼 가이드라인
- [iOS Human Interface Guidelines - Typography](https://developer.apple.com/design/human-interface-guidelines/typography)
- [iOS Font Size Guidelines](https://www.learnui.design/blog/ios-font-size-guidelines.html)
- [Material Design Typography](https://m3.material.io/styles/typography/type-scale-tokens)

### 폰트
- **Toss Product Sans**: 토스 공식 폰트 (비공개)
- **Pretendard**: 유사한 오픈소스 한글 폰트
- **Inter**: 영문 폰트 (현재 프로젝트 사용 중)

---

## 12. 현재 프로젝트 권장 수정 사항

### Typography Scale 조정 (영어 앱 기준)

```dart
// toss_text_styles.dart 수정 권장

// Body (기본 본문)
static TextStyle get body => fontSize: 16, height: 1.5

// Body Small (보조 본문)
static TextStyle get bodySmall => fontSize: 14, height: 1.5

// Caption (캡션)
static TextStyle get caption => fontSize: 13, height: 1.4

// Subtitle (강조 본문)
static TextStyle get subtitle => fontSize: 17, height: 1.5
```

### 적용 패턴

```dart
// 주요 텍스트
Text(
  'Balance',
  style: TossTextStyles.body.copyWith(
    color: TossColors.gray900,
    fontSize: 16,
    fontWeight: FontWeight.w600,
  ),
)

// 보조 텍스트
Text(
  'Total Journal',
  style: TossTextStyles.body.copyWith(
    color: TossColors.gray700,
    fontSize: 16,
  ),
)

// 설명 텍스트
Text(
  '50% of total balance',
  style: TossTextStyles.body.copyWith(
    color: TossColors.gray600,
    fontSize: 14,
  ),
)
```

---

---

## 13. 현재 앱 테마 vs 가이드라인 차이 분석

### Typography (toss_text_styles.dart)

| 항목 | 현재 값 | 권장 값 | 상태 | 비고 |
|------|---------|---------|------|------|
| **body fontSize** | 14px | 16px | ⚠️ 수정 필요 | 영어 앱은 16px 권장 (iOS 17pt) |
| **body height** | 1.429 | 1.5 | ⚠️ 수정 필요 | 토스 표준 1.5배 |
| **bodyLarge** | 14px (body와 동일) | 삭제 또는 16px | ⚠️ 중복 | body와 동일하여 의미 없음 |
| **bodySmall** | 13px | 14px | ⚠️ 수정 필요 | |
| **subtitle** | 16px | 17px | ⚠️ 수정 필요 | 토스 F17 표준 |
| **caption** | 12px | 13px | ⚠️ 수정 필요 | |
| **button** | 14px | 16px | ⚠️ 수정 필요 | 버튼 텍스트도 16px 권장 |

### Spacing (toss_spacing.dart)

| 항목 | 현재 값 | 권장 값 | 상태 | 비고 |
|------|---------|---------|------|------|
| **space6** | 30px | 24px | ⚠️ 오류 | 6 × 4 = 24px여야 함 |
| **space7** | 28px | - | ⚠️ 순서 오류 | space6(30) > space7(28) 문제 |
| 나머지 | 정상 | - | ✅ 정상 | 4px 그리드 준수 |

**수정 필요:**
```dart
// 현재 (잘못됨)
static const double space6 = 30.0;   // 6x base
static const double space7 = 28.0;   // 7x base

// 권장 (수정)
static const double space6 = 24.0;   // 6 × 4 = 24px
static const double space7 = 28.0;   // 7 × 4 = 28px
```

### Colors (toss_colors.dart)

| 항목 | 현재 값 | 토스 공식 | 상태 |
|------|---------|-----------|------|
| **primary** | #0064FF | #0064FF | ✅ 정상 |
| **success** | #00C896 | #00C896 | ✅ 정상 |
| **error** | #FF5847 | #FF5847 | ✅ 정상 |
| **warning** | #FF9500 | #FF9500 | ✅ 정상 |
| **gray scale** | Bootstrap 기반 | 토스 공식과 다름 | ⚠️ 검토 필요 |

**Grayscale 비교:**
```dart
// 현재 (Bootstrap 기반)
gray900: #212529
gray800: #343A40
gray700: #495057
gray600: #6C757D

// 토스 공식 (참고용)
gray900: #202632
gray800: #2E3440
gray700: #505866
gray600: #8B95A1
```

### Border Radius (toss_border_radius.dart)

| 항목 | 현재 값 | 상태 |
|------|---------|------|
| **xs** | 4px | ✅ 정상 |
| **sm** | 6px | ✅ 정상 |
| **md** | 8px | ✅ 정상 |
| **lg** | 12px | ✅ 정상 |
| **xl** | 16px | ✅ 정상 |
| **xxl** | 20px | ✅ 정상 |
| **card** | 12px | ✅ 정상 |

### Shadows (toss_shadows.dart)

| 항목 | 현재 값 | 상태 |
|------|---------|------|
| **card shadow** | 4% opacity | ✅ 정상 (토스 스타일) |
| **elevation levels** | 4-8% | ✅ 정상 |
| **minimal shadow 철학** | 준수 | ✅ 정상 |

### Animations (toss_animations.dart)

| 항목 | 현재 값 | 상태 |
|------|---------|------|
| **fast** | 150ms | ✅ 정상 |
| **normal** | 200ms | ✅ 정상 |
| **medium** | 250ms | ✅ 정상 |
| **No Bounce** | 준수 | ✅ 정상 |
| **standard curve** | easeInOutCubic | ✅ 정상 |

---

## 14. 수정 우선순위

### 🔴 높음 (즉시 수정 권장)

1. **Typography Scale 조정** (영어 앱 기준)
   ```dart
   // toss_text_styles.dart
   body: fontSize 14 → 16, height 1.429 → 1.5
   bodySmall: fontSize 13 → 14
   subtitle: fontSize 16 → 17
   caption: fontSize 12 → 13
   button: fontSize 14 → 16
   ```

2. **Spacing 오류 수정**
   ```dart
   // toss_spacing.dart
   space6: 30.0 → 24.0
   // space7은 28.0 유지
   ```

### 🟡 중간 (검토 필요)

3. **bodyLarge 정리**
   - 현재 body와 동일하여 의미 없음
   - 삭제하거나 16px로 차별화

4. **Grayscale 검토**
   - Bootstrap 기반 vs 토스 공식
   - 현재 사용 중인 경우 변경 시 영향도 분석 필요

### 🟢 낮음 (현재 상태 유지 가능)

5. Colors, BorderRadius, Shadows, Animations
   - 현재 토스 스타일과 일치
   - 변경 불필요

---

## 15. 수정 후 예상 효과

### Before (현재)
```dart
Text(
  'Balance',
  style: TossTextStyles.body.copyWith(
    fontSize: 16,  // 매번 override 필요
    fontWeight: FontWeight.w600,
  ),
)
```

### After (수정 후)
```dart
Text(
  'Balance',
  style: TossTextStyles.body.copyWith(
    fontWeight: FontWeight.w600,
  ),
  // fontSize 16이 기본값이므로 override 불필요
)
```

**장점:**
- 코드 간결화 (`fontSize: 16` 반복 제거)
- 일관성 향상 (테마에서 중앙 관리)
- 유지보수 용이 (한 곳에서 전체 변경)

---

*이 가이드는 토스 디자인 시스템 조사 결과를 바탕으로 작성되었습니다.*
*토스의 공식 TDS는 비공개이므로, 공개된 자료와 플랫폼 가이드라인을 참고하였습니다.*
