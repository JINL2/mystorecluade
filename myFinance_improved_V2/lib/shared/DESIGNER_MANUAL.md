# 🎨 디자인 시스템 & Widgetbook 매뉴얼
## 디자이너 & 개발자를 위한 완전 가이드

---

# 📁 Part 1: 폴더 구조 이해하기

## 우리 앱의 디자인 부품 창고

```
shared/
│
├── 🎨 themes/                    ← 색상, 글자, 간격 등 기본 설정
│   ├── toss_colors.dart          ← 모든 색상 정의
│   ├── toss_text_styles.dart     ← 모든 글자 스타일
│   ├── toss_spacing.dart         ← 간격 (8px, 16px 등)
│   ├── toss_border_radius.dart   ← 모서리 둥글기
│   ├── toss_shadows.dart         ← 그림자 효과
│   └── toss_icons.dart           ← 아이콘 모음
│
└── 🧩 widgets/                   ← 실제 UI 부품들
    │
    ├── 📦 atoms/                 ← 가장 작은 부품 (레고 1개 블록)
    │   ├── buttons/              ← 버튼들
    │   ├── display/              ← 보여주기용 (카드, 칩, 뱃지)
    │   ├── feedback/             ← 상태 알림 (로딩, 에러, 빈화면)
    │   ├── inputs/               ← 입력창
    │   └── layout/               ← 배치용
    │
    ├── 🧬 molecules/             ← 작은 부품 조합 (레고 2-3개 조합)
    │   ├── buttons/              ← 복합 버튼 (FAB 등)
    │   ├── cards/                ← 복합 카드
    │   ├── inputs/               ← 복합 입력 (드롭다운 등)
    │   ├── navigation/           ← 네비게이션 (앱바, 탭바)
    │   └── keyboard/             ← 키보드 관련
    │
    ├── 🏗️ organisms/             ← 큰 부품 조합 (레고 집 한 채)
    │   ├── calendars/            ← 캘린더
    │   ├── dialogs/              ← 대화상자
    │   ├── sheets/               ← 바텀시트
    │   ├── pickers/              ← 선택기 (날짜, 시간)
    │   └── shift/                ← 근무 관련
    │
    ├── 📐 templates/             ← 페이지 뼈대
    │   └── toss_scaffold.dart    ← 기본 페이지 구조
    │
    └── 🤖 ai/                    ← AI 관련 위젯
        └── ai_chat/              ← AI 채팅
```

## Widgetbook 폴더 구조

```
lib/widgetbook/
│
├── main.dart                     ← Widgetbook 앱 시작점
│
└── use_cases/                    ← 각 위젯의 미리보기 정의
    │
    ├── atoms/                    ← Atoms 위젯 미리보기
    │   ├── atoms_directory.dart  ← Atoms 목록 관리
    │   ├── buttons/              ← 버튼 미리보기들
    │   │   ├── toss_button_use_case.dart
    │   │   └── toggle_button_use_case.dart
    │   ├── display/              ← Display 미리보기들
    │   ├── feedback/             ← Feedback 미리보기들
    │   ├── inputs/               ← Input 미리보기들
    │   └── layout/               ← Layout 미리보기들
    │
    └── molecules/                ← Molecules 위젯 미리보기
        ├── molecules_directory.dart
        ├── buttons/
        ├── cards/
        ├── inputs/
        └── navigation/
```

---

## 🔢 Atomic Design 이해하기

**레고로 비유하면:**

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  Atoms (원자)        =  레고 블록 1개                           │
│  예: 버튼 1개, 텍스트 필드 1개                                  │
│                                                                 │
│  Molecules (분자)    =  레고 블록 2-3개 조합                    │
│  예: 검색창 + 버튼 = 검색바                                     │
│                                                                 │
│  Organisms (유기체)  =  레고로 만든 집 1채                      │
│  예: 캘린더, 전체 대화상자                                      │
│                                                                 │
│  Templates (템플릿)  =  집들이 모인 마을 구조                   │
│  예: 전체 페이지 레이아웃                                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

# 🧩 Part 2: 현재 우리가 가진 부품들

## 📦 Atoms (원자) - 21개

### 🔘 Buttons (버튼) - 4개
| 파일명 | 설명 | 용도 |
|--------|------|------|
| `toss_button.dart` | 메인 버튼 | Primary, Secondary, Outlined, Text 4종류 |
| `toss_primary_button.dart` | 파란색 주요 버튼 | 저장, 확인 등 |
| `toss_secondary_button.dart` | 회색 보조 버튼 | 취소, 뒤로 등 |
| `toggle_button.dart` | 토글 버튼 | 켜기/끄기 |

### 📋 Display (표시) - 6개
| 파일명 | 설명 | 용도 |
|--------|------|------|
| `toss_card.dart` | 기본 카드 | 정보 담는 박스 |
| `toss_card_safe.dart` | 안전한 카드 | 에러 방지 카드 |
| `toss_chip.dart` | 칩/태그 | 카테고리 표시 |
| `toss_badge.dart` | 뱃지 | 알림 숫자 표시 |
| `employee_profile_avatar.dart` | 프로필 아바타 | 직원 사진 |
| `cached_product_image.dart` | 캐시 이미지 | 상품 이미지 |

### 💬 Feedback (피드백) - 4개
| 파일명 | 설명 | 용도 |
|--------|------|------|
| `toss_loading_view.dart` | 로딩 화면 | 데이터 불러올 때 |
| `toss_empty_view.dart` | 빈 화면 | 데이터 없을 때 |
| `toss_error_view.dart` | 에러 화면 | 오류 발생 시 |
| `toss_refresh_indicator.dart` | 새로고침 | 당겨서 새로고침 |

### ✏️ Inputs (입력) - 3개
| 파일명 | 설명 | 용도 |
|--------|------|------|
| `toss_text_field.dart` | 텍스트 입력창 | 일반 글자 입력 |
| `toss_search_field.dart` | 검색창 | 검색 기능 |
| `toss_enhanced_text_field.dart` | 향상된 입력창 | 더 많은 기능 |

### 📐 Layout (레이아웃) - 2개
| 파일명 | 설명 | 용도 |
|--------|------|------|
| `toss_section_header.dart` | 섹션 헤더 | 구역 제목 |
| `gray_divider_space.dart` | 회색 구분선 | 영역 구분 |

---

## 🧬 Molecules (분자) - 15개

### 🔘 Buttons - 2개
| 파일명 | 설명 |
|--------|------|
| `toss_fab.dart` | 플로팅 액션 버튼 (둥근 + 버튼) |
| `toss_speed_dial.dart` | 펼쳐지는 FAB 메뉴 |

### 📋 Cards - 2개
| 파일명 | 설명 |
|--------|------|
| `toss_white_card.dart` | 흰색 배경 카드 |
| `toss_expandable_card.dart` | 펼쳐지는 카드 |

### ✏️ Inputs - 5개
| 파일명 | 설명 |
|--------|------|
| `toss_dropdown.dart` | 드롭다운 선택 |
| `toss_quantity_input.dart` | 숫자 입력 (+/-) |
| `toss_quantity_stepper.dart` | 단계별 숫자 조절 |
| `toss_enhanced_text_field.dart` | 향상된 텍스트 필드 |
| `category_chip.dart` | 카테고리 선택 칩 |

### 🧭 Navigation - 2개
| 파일명 | 설명 |
|--------|------|
| `toss_app_bar_1.dart` | 상단 앱바 |
| `toss_tab_bar_1.dart` | 탭 바 |

### ⌨️ Keyboard - 4개
| 파일명 | 설명 |
|--------|------|
| `toss_keyboard_toolbar.dart` | 키보드 툴바 |
| `keyboard_toolbar_1.dart` | 키보드 툴바 v1 |
| `toss_currency_exchange_modal.dart` | 환율 계산 모달 |
| `toss_textfield_keyboard_modal.dart` | 텍스트필드 키보드 |

---

## 🏗️ Organisms (유기체) - 13개

### 📅 Calendars - 6개
| 파일명 | 설명 |
|--------|------|
| `toss_month_calendar.dart` | 월간 캘린더 |
| `toss_month_navigation.dart` | 월 이동 네비게이션 |
| `toss_week_navigation.dart` | 주 이동 네비게이션 |
| `month_dates_picker.dart` | 월간 날짜 선택 |
| `week_dates_picker.dart` | 주간 날짜 선택 |
| `calendar_time_range.dart` | 기간 선택 |

### 💬 Dialogs - 3개
| 파일명 | 설명 |
|--------|------|
| `toss_confirm_cancel_dialog.dart` | 확인/취소 대화상자 |
| `toss_info_dialog.dart` | 정보 대화상자 |
| `toss_success_error_dialog.dart` | 성공/에러 대화상자 |

### 📋 Sheets - 2개
| 파일명 | 설명 |
|--------|------|
| `toss_bottom_sheet.dart` | 기본 바텀시트 |
| `toss_selection_bottom_sheet.dart` | 선택용 바텀시트 |

### 🎯 Pickers - 2개
| 파일명 | 설명 |
|--------|------|
| `toss_date_picker.dart` | 날짜 선택기 |
| `toss_time_picker.dart` | 시간 선택기 |

---

## 📐 Templates (템플릿) - 1개

| 파일명 | 설명 |
|--------|------|
| `toss_scaffold.dart` | 기본 페이지 뼈대 |

---

# 🎨 Part 3: 테마 (색상, 글자, 간격)

## 🎨 Colors (색상) - toss_colors.dart

```
┌────────────────────────────────────────────────────────────┐
│  Primary (주요색)                                          │
│  ├── TossColors.primary      → 메인 파란색                 │
│  ├── TossColors.primaryLight → 연한 파란색                 │
│  └── TossColors.primaryDark  → 진한 파란색                 │
│                                                            │
│  Secondary (보조색)                                        │
│  ├── TossColors.secondary    → 보조 색상                   │
│  └── ...                                                   │
│                                                            │
│  Gray Scale (회색 계열)                                    │
│  ├── TossColors.gray100      → 가장 연한 회색              │
│  ├── TossColors.gray200      → 연한 회색                   │
│  ├── TossColors.gray300      → 중간 회색                   │
│  └── TossColors.gray900      → 가장 진한 회색              │
│                                                            │
│  Semantic (의미있는 색)                                    │
│  ├── TossColors.success      → 성공 (초록)                 │
│  ├── TossColors.warning      → 경고 (노랑)                 │
│  ├── TossColors.error        → 에러 (빨강)                 │
│  └── TossColors.info         → 정보 (파랑)                 │
└────────────────────────────────────────────────────────────┘
```

## 🔤 Text Styles (글자 스타일) - toss_text_styles.dart

```
┌────────────────────────────────────────────────────────────┐
│  TossTextStyles.h1         → 가장 큰 제목                  │
│  TossTextStyles.h2         → 큰 제목                       │
│  TossTextStyles.h3         → 중간 제목                     │
│  TossTextStyles.h4         → 작은 제목                     │
│  TossTextStyles.body       → 본문                          │
│  TossTextStyles.bodySmall  → 작은 본문                     │
│  TossTextStyles.caption    → 캡션/설명                     │
│  TossTextStyles.button     → 버튼 글자                     │
└────────────────────────────────────────────────────────────┘
```

## 📏 Spacing (간격) - toss_spacing.dart

```
┌────────────────────────────────────────────────────────────┐
│  TossSpacing.xxs  = 4px    → 아주 작은 간격                │
│  TossSpacing.xs   = 8px    → 작은 간격                     │
│  TossSpacing.sm   = 12px   → 약간 작은 간격                │
│  TossSpacing.md   = 16px   → 기본 간격                     │
│  TossSpacing.lg   = 24px   → 큰 간격                       │
│  TossSpacing.xl   = 32px   → 더 큰 간격                    │
│  TossSpacing.xxl  = 48px   → 아주 큰 간격                  │
└────────────────────────────────────────────────────────────┘
```

## ⭕ Border Radius (모서리 둥글기) - toss_border_radius.dart

```
┌────────────────────────────────────────────────────────────┐
│  TossBorderRadius.xs   = 4px   → 살짝 둥글게               │
│  TossBorderRadius.sm   = 8px   → 작게 둥글게               │
│  TossBorderRadius.md   = 12px  → 보통 둥글게               │
│  TossBorderRadius.lg   = 16px  → 많이 둥글게               │
│  TossBorderRadius.xl   = 24px  → 아주 둥글게               │
│  TossBorderRadius.full = 999px → 완전 원형                 │
└────────────────────────────────────────────────────────────┘
```

---

# 📖 Part 4: Widgetbook 사용법 (디자이너용)

## Widgetbook이 뭐예요?

**레고 카탈로그**를 생각해보세요!

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   레고 카탈로그 📕          =    Widgetbook 📱          │
│                                                         │
│   • 모든 부품이 정리됨      =    모든 버튼/카드 정리됨   │
│   • 부품별로 사진 있음      =    위젯별로 미리보기       │
│   • 색상/크기 옵션 표시     =    속성 조절 가능         │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 Widgetbook 실행하기

### 1단계: 터미널 열기
Mac에서 `터미널` 앱을 엽니다.

### 2단계: 프로젝트 폴더로 이동
```bash
cd /Applications/XAMPP/xamppfiles/htdocs/mysite/mystorecluade/myFinance_improved_V2
```

### 3단계: Widgetbook 실행
```bash
flutter run -t lib/widgetbook/main.dart
```

### 4단계: 기다리기
처음에는 좀 오래 걸려요 (1-2분).
"앱이 실행됨" 메시지가 나오면 성공!

---

## 🖥️ Widgetbook 화면 설명

```
┌────────────────────────────────────────────────────────────────┐
│                        Widgetbook                              │
├──────────────┬─────────────────────────────────────────────────┤
│              │                                                 │
│  📁 Atoms    │     ┌─────────────────────────┐                │
│    📁 Buttons│     │                         │                │
│      • Toss  │     │    [ 저장하기 ]         │  ← 실제 버튼   │
│        Button│     │                         │                │
│    📁 Display│     │                         │                │
│      • Card  │     └─────────────────────────┘                │
│      • Chip  │                                                 │
│              │  ─────────────────────────────────────────────  │
│  📁 Molecules│                                                 │
│    📁 Cards  │  🎛️ Knobs (조절판)                              │
│    📁 Buttons│  ┌─────────────────────────────────────────┐   │
│              │  │ Text: [저장하기        ]                │   │
│              │  │ Enabled: [✓]                            │   │
│              │  │ Loading: [ ]                            │   │
│              │  │ Full Width: [✓]                         │   │
│              │  └─────────────────────────────────────────┘   │
│              │                                                 │
├──────────────┴─────────────────────────────────────────────────┤
│  📱 iPhone 13  |  🔤 1.0x  |  ☀️ Light                         │
└────────────────────────────────────────────────────────────────┘

     왼쪽          =    부품 목록 (폴더처럼 정리됨)
     가운데        =    부품 미리보기 (실제로 어떻게 보이는지)
     오른쪽 아래   =    조절판 (버튼 글자, 색상 등 바꿔보기)
     맨 아래       =    폰 종류, 글자 크기, 테마 선택
```

---

## 🎛️ Knobs(조절판) 사용법

**Knobs = 버튼의 옵션을 실시간으로 바꿔보는 조절판**

예를 들어 버튼의:
- 글자를 "저장" → "확인"으로 바꾸기
- 비활성화 상태로 바꾸기
- 로딩 중 상태로 바꾸기

```
┌─────────────────────────────────────────┐
│ 🎛️ Knobs                                │
├─────────────────────────────────────────┤
│                                         │
│  Text (글자)                            │
│  ┌─────────────────────────────┐        │
│  │ 저장하기                    │        │  ← 여기 글자 바꾸면
│  └─────────────────────────────┘        │     버튼 글자가 바뀜!
│                                         │
│  Enabled (활성화)                       │
│  [✓] ← 체크 해제하면 버튼 비활성화      │
│                                         │
│  Loading (로딩중)                       │
│  [ ] ← 체크하면 로딩 애니메이션!        │
│                                         │
│  Full Width (전체 너비)                 │
│  [✓] ← 체크하면 버튼이 화면 꽉 채움    │
│                                         │
└─────────────────────────────────────────┘
```

---

## 📱 디바이스 미리보기

화면 아래에서 다양한 폰으로 테스트:

| 기기 | 화면 크기 | 용도 |
|------|----------|------|
| iPhone 13 | 보통 | 일반 아이폰 |
| iPhone 13 Pro Max | 큰 화면 | 큰 아이폰 |
| iPhone SE | 작은 화면 | 작은 아이폰 |
| Samsung Galaxy S20 | 안드로이드 | 삼성폰 |
| Small Phone | 아주 작음 | 작은 폰 테스트 |

---

## 🔤 글자 크기 테스트

노인분들이나 시력이 안 좋은 분들을 위해
큰 글자 설정했을 때 어떻게 보이는지 확인!

| 배율 | 의미 |
|------|------|
| 1.0x | 기본 크기 |
| 1.25x | 조금 큼 |
| 1.5x | 꽤 큼 |
| 2.0x | 아주 큼 (접근성 테스트용) |

---

# 🛠️ Part 5: 위젯 추가/수정/관리 (개발자용)

## 📝 새 위젯 추가하기

### Step 1: 위젯 파일 만들기

**어디에 만들어야 할까요?**
```
위젯 크기에 따라 폴더 선택:

• 가장 작은 부품 (버튼, 텍스트필드 등)
  → shared/widgets/atoms/[카테고리]/

• 작은 조합 (앱바, 드롭다운 등)
  → shared/widgets/molecules/[카테고리]/

• 큰 조합 (캘린더, 대화상자 등)
  → shared/widgets/organisms/[카테고리]/
```

**예시: 새 버튼 만들기**
```
shared/widgets/atoms/buttons/toss_icon_button.dart
```

### Step 2: Widgetbook Use Case 만들기

**1. use_case 파일 생성**

```
lib/widgetbook/use_cases/atoms/buttons/toss_icon_button_use_case.dart
```

**2. 코드 작성 (복사해서 수정하세요)**

```dart
import 'package:flutter/material.dart';
import 'package:widgetbook/widgetbook.dart';
import 'package:myfinance_improved/shared/widgets/atoms/buttons/toss_icon_button.dart';

final tossIconButtonComponent = WidgetbookComponent(
  name: 'TossIconButton',  // ← 카탈로그에 표시될 이름
  useCases: [
    WidgetbookUseCase(
      name: 'Default',     // ← 이 버튼의 기본 상태
      builder: (context) => Padding(
        padding: const EdgeInsets.all(16),
        child: TossIconButton(
          // Knobs로 조절 가능하게 만들기
          icon: Icons.add,
          onPressed: () {},
        ),
      ),
    ),
    // 다른 상태들 추가 가능
    WidgetbookUseCase(
      name: 'Disabled',    // ← 비활성화 상태
      builder: (context) => Padding(
        padding: const EdgeInsets.all(16),
        child: TossIconButton(
          icon: Icons.add,
          onPressed: null,  // null이면 비활성화
        ),
      ),
    ),
  ],
);
```

### Step 3: Directory에 등록하기

**atoms_directory.dart 열기:**
```
lib/widgetbook/use_cases/atoms/atoms_directory.dart
```

**import 추가:**
```dart
import 'buttons/toss_icon_button_use_case.dart';  // ← 추가
```

**폴더에 등록:**
```dart
WidgetbookFolder(
  name: 'Buttons',
  children: [
    tossButtonComponent,
    toggleButtonComponent,
    tossIconButtonComponent,  // ← 추가
  ],
),
```

### Step 4: 확인하기

```bash
flutter run -t lib/widgetbook/main.dart
```

왼쪽 메뉴에서 새 위젯이 보이면 성공!

---

## ✏️ 기존 위젯 수정하기

### 위젯 디자인 수정

**1. 실제 위젯 파일 수정**
```
shared/widgets/atoms/buttons/toss_button.dart
```
→ 여기서 색상, 크기, 모양 등 수정

**2. Widgetbook에서 확인**
```bash
flutter run -t lib/widgetbook/main.dart
```
→ 수정된 내용이 바로 반영됨!

### 새로운 Use Case 추가

기존 위젯에 새로운 상태를 보여주고 싶을 때:

```dart
// toss_button_use_case.dart에 추가

WidgetbookUseCase(
  name: 'With Icon',  // ← 새로운 상태
  builder: (context) => Padding(
    padding: const EdgeInsets.all(16),
    child: TossButton.primary(
      text: 'Download',
      icon: Icons.download,  // 아이콘 있는 버튼
      onPressed: () {},
    ),
  ),
),
```

---

## 🗑️ 위젯 삭제하기

### Step 1: Directory에서 제거
```dart
// atoms_directory.dart

WidgetbookFolder(
  name: 'Buttons',
  children: [
    tossButtonComponent,
    toggleButtonComponent,
    // tossIconButtonComponent,  ← 이 줄 삭제 또는 주석
  ],
),
```

### Step 2: Use Case 파일 삭제 (선택)
```
lib/widgetbook/use_cases/atoms/buttons/toss_icon_button_use_case.dart
```

### Step 3: 실제 위젯 삭제 (선택)
```
shared/widgets/atoms/buttons/toss_icon_button.dart
```

---

## 🎛️ Knobs 종류와 사용법

Knobs = 디자이너가 실시간으로 옵션을 바꿔볼 수 있게 해주는 것

### 📝 텍스트 입력
```dart
context.knobs.string(
  label: 'Button Text',      // 표시될 이름
  initialValue: '저장하기',   // 기본값
)
```

### ✅ 체크박스 (켜기/끄기)
```dart
context.knobs.boolean(
  label: 'Enabled',
  initialValue: true,
)
```

### 🔢 숫자 슬라이더
```dart
context.knobs.int.slider(
  label: 'Count',
  initialValue: 1,
  min: 0,
  max: 10,
)
```

### 📋 선택 목록 (드롭다운)
```dart
context.knobs.list(
  label: 'Size',
  options: ['Small', 'Medium', 'Large'],
  initialOption: 'Medium',
)
```

### 🎨 색상 선택
```dart
context.knobs.color(
  label: 'Background Color',
  initialValue: Colors.blue,
)
```

---

## 📁 파일 구조 요약

```
새 위젯 추가할 때 수정해야 하는 파일들:

1️⃣ 위젯 파일 생성
   shared/widgets/[atoms|molecules|organisms]/[카테고리]/새위젯.dart

2️⃣ Use Case 파일 생성
   lib/widgetbook/use_cases/[atoms|molecules]/[카테고리]/새위젯_use_case.dart

3️⃣ Directory 파일 수정
   lib/widgetbook/use_cases/[atoms|molecules]/[atoms|molecules]_directory.dart
   → import 추가
   → children 배열에 추가
```

---

## 🔄 자주 쓰는 명령어

```bash
# Widgetbook 실행
flutter run -t lib/widgetbook/main.dart

# 실행 중 새로고침 (코드 수정 후)
# 터미널에서 'r' 키 누르기

# 완전 재시작 (에러 날 때)
# 터미널에서 'R' 키 누르기 (대문자)

# 종료
# 터미널에서 'q' 키 누르기
```

---

# ✅ Part 6: 디자인 체크리스트

Widgetbook에서 이것들을 확인하세요:

## 버튼 체크
- [ ] 글자가 너무 길 때 잘리지 않나요?
- [ ] 비활성화 상태가 명확히 구분되나요?
- [ ] 로딩 중일 때 애니메이션이 보이나요?

## 카드 체크
- [ ] 내용이 많을 때 레이아웃이 깨지지 않나요?
- [ ] 그림자가 적절한가요?

## 입력창 체크
- [ ] 에러 메시지가 잘 보이나요?
- [ ] 힌트 텍스트가 명확한가요?

## 반응형 체크
- [ ] 작은 폰에서도 잘 보이나요?
- [ ] 큰 폰에서 너무 허전하지 않나요?
- [ ] 글자 크기를 키워도 괜찮나요?

---

# 🆘 Part 7: 문제가 생겼을 때

### "flutter 명령어를 찾을 수 없음"
→ 개발자에게 Flutter 설치 요청하세요

### 화면이 안 나와요
→ 터미널에서 `r` 키를 눌러 새로고침

### 앱이 멈췄어요
→ 터미널에서 `Ctrl + C` 누르고 다시 실행

### 빌드 에러 (빨간 글씨)
→ 에러 메시지 스크린샷 찍어서 개발자에게 공유

### 위젯이 카탈로그에 안 보여요
→ directory 파일에 등록했는지 확인

---

# 📞 Part 8: 위젯 변경 요청하기

개발팀에 연락할 때:

1. **어떤 위젯**인지 (예: TossButton)
2. **어떤 변경**이 필요한지 (예: 색상 변경)
3. **스크린샷**이 있으면 더 좋아요!

**요청 예시:**
```
"atoms/buttons/toss_button.dart의 Primary 버튼 색상을
파란색에서 초록색으로 바꿔주세요"
```

---

# 🎯 요약

## 디자이너용 요약
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  🚀 Widgetbook 실행:                                    │
│     1. 터미널 열기                                      │
│     2. cd /Applications/.../myFinance_improved_V2       │
│     3. flutter run -t lib/widgetbook/main.dart          │
│                                                         │
│  🎛️ 사용법:                                             │
│     • 왼쪽에서 부품 선택                                │
│     • Knobs로 옵션 조절                                 │
│     • 다양한 폰/글자크기로 테스트                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 개발자용 요약
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  ➕ 새 위젯 추가:                                       │
│     1. shared/widgets/[레벨]/[카테고리]/위젯.dart       │
│     2. lib/widgetbook/use_cases/.../위젯_use_case.dart  │
│     3. directory.dart에 import & 등록                   │
│                                                         │
│  ✏️ 위젯 수정:                                          │
│     • 위젯 파일 수정 → Widgetbook에서 바로 확인         │
│                                                         │
│  🗑️ 위젯 삭제:                                          │
│     • directory.dart에서 제거                           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

**끝! 이게 전부예요! 🎉**

---

*마지막 업데이트: 2025-12-31*
