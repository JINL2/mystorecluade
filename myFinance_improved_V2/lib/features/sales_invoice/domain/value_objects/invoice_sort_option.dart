/// Invoice sort option enum
/// Note: UI display properties (displayName, icon) are in presentation/extensions/invoice_sort_option_extension.dart
enum InvoiceSortOption {
  date,
  invoiceId,
  user,
  products,
  amount,
}
