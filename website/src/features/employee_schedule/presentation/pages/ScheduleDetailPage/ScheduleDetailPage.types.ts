/**
 * ScheduleDetailPage Component Types
 */

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface ScheduleDetailPageProps {
  // No props needed - uses URL params and AppState context
}
