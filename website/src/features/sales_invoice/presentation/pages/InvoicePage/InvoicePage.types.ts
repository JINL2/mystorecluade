/**
 * InvoicePage Component Types
 */

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface InvoicePageProps {
  // No props needed - uses AppState for companyId and localStorage for storeId
}
